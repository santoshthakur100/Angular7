import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { ProductCumImageComponent } from './productCumImage.component';

@NgModule({
  imports: [
    CommonModule
  ],
  declarations: [ProductCumImageComponent],
  exports: [ProductCumImageComponent]

})
export class ProductCumImageModule { }
